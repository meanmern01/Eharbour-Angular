export interface CustomDataTableHeader {
    title: string;
    property: string;
    dataType?: string;
}

export interface MenuListObj {
    icon: string;
    menuid: number;
    order_no: number;
    parent: string;
    title: string;
    url: string;
}